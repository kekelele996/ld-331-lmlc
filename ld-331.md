<!-- 改编自：医疗健康主题项目提示词/ld-331/ld-331.md；后端由 Spring Boot (Java 17) 改造为 Go 1.22 + Gin + GORM -->
请根据以下要求生成项目代码和 README。

## 项目主要需求

**项目概述**：医院智能排班系统——为医院各科室提供医护人员的自动化排班管理，支持规则引擎排班、调班申请、班次统计与冲突检测。
**项目类型**：全栈Web应用

### 功能需求

1. **科室与岗位管理**：管理员可维护医院科室树结构，每个科室下设不同岗位（主任医师、主治医师、住院医师、护士长、护士等），每个岗位配置编制人数和技能标签。

2. **排班规则引擎**：支持配置排班规则（连续工作天数上限、周末排班轮循、节假日排班优先级、夜班后不能接白班等），系统根据规则自动生成周/月排班表，支持一键生成和手动微调。

3. **可视化排班表**：以日历或甘特图形式展示各科室排班表，不同班次用颜色区分（白班、夜班、中班、休息），支持按科室、岗位、人员筛选查看，支持拖拽调整排班。

4. **调班与替班申请**：医护人员可发起调班申请（与同事换班），填写原因并指定替班人，主管审批通过后系统自动更新排班表，调班记录留痕可追溯。

5. **出勤与工时统计**：系统自动统计每位医护人员的月度出勤天数、各类班次数量、加班时长，生成个人和科室的排班统计报表，支持导出 Excel。

6. **节假日与特殊日期**：支持维护法定节假日、医院特殊工作日的排班策略（节假日排班系数、双倍计班规则），特殊日期自动标红提醒。

### 技术要求

- **英文短名（用于 Docker Compose）**：`gbsched`
- **前端端口**：`FRONTEND_PORT=18931`
- **后端端口**：`BACKEND_PORT=19931`
- **前端框架**：Vue 3 + TypeScript + Element Plus
- **构建工具**：Vite
- **后端框架**：Go 1.22 + Gin + GORM
- **数据库**：MySQL 8.0
- **ORM**：GORM
- **认证方式**：JWT + 角色权限控制（使用 `github.com/golang-jwt/jwt/v5`）
- **其他依赖**：甘特图组件（如 gantt-elastic 或自研）、`github.com/gin-gonic/gin`、`gorm.io/gorm`、`gorm.io/driver/mysql`、`golang.org/x/crypto/bcrypt`、Excel 导出（如 `github.com/xuri/excelize/v2`）

## 代码存放规则

这是前后端分离项目。

前端代码放在 `frontend/`：
页面、组件、路由、状态管理、API 请求、类型定义、样式和资源等放在对应的子文件夹

后端代码放在 `backend/`：
数据库脚本放在 `database/`。

后端采用 Go 标准布局，必须包含：
```
backend/
├── go.mod
├── go.sum
├── cmd/server/main.go
└── internal/
    ├── config/
    ├── model/
    ├── repository/
    ├── service/
    ├── handler/
    ├── router/
    ├── middleware/
    ├── dto/
    └── constants/
```
其中 model/repository/service/handler/router 等按实体（Department、Position、Staff、Schedule、Shift、ShiftRequest、ScheduleRule、Holiday 等）分文件，禁止把多个实体的 CRUD 合并到单一文件。
## Go 企业级工程规范（必须遵循）

后端 Go 代码除满足上述功能与分层要求外，还必须遵循当前 Go 语言通用的企业级工程规范：

### 1. 项目目录规范
- 根目录 `go.mod` 声明 Go 版本 `go 1.22`，模块名使用公司/项目规范（如 `github.com/<org>/<project>`）。
- 采用 `cmd/` + `internal/` + `pkg/` 布局：`cmd/server/main.go` 只负责加载配置、装配依赖、启动服务；私有业务代码全部放在 `internal/`；仅无业务依赖的可复用库放 `pkg/`。
- 数据库迁移脚本放 `migrations/`，接口定义/OpenAPI 文档放 `api/`，Docker/K8s 部署文件放 `deploy/`。
- 包名使用小写单词、不加下划线；文件使用 `snake_case.go`；导出标识符用 `CamelCase`，未导出的用 `camelCase`；禁止使用拼音命名。

### 2. 分层与依赖方向
- 依赖方向严格单向：`handler → service → repository → model`，`model` 不依赖任何上层，禁止反向引用与包循环依赖。
- 每个核心实体按 `model_<entity>.go`、`repository_<entity>.go`、`service_<entity>.go`、`handler_<entity>.go` 分文件，职责单一。
- 依赖通过构造函数注入：`func NewXService(repo Repo, logger *slog.Logger) *XService`；禁止在业务代码中全局 `init` 连接数据库或散落 `sql.Open`。

### 3. 错误处理
- 使用 `fmt.Errorf("list users: %w", err)` 包裹并透传错误，保留错误链。
- 仓储层定义哨兵错误（如 `var ErrNotFound = errors.New("not found")`），上层用 `errors.Is` 判断。
- 禁止在 service 层打印日志后吞掉错误；错误统一由 `middleware` 或 `handler` 转换为标准 JSON 响应。
- 所有 error 必须被处理，关键错误禁止 `_ =` 忽略。

### 4. 配置与日志
- 配置通过环境变量注入，在 `internal/config/config.go` 集中解析，推荐使用 `github.com/caarlos0/env/v11` 或 `spf13/viper`。
- 日志使用 `log/slog` 或 `go.uber.org/zap` 结构化输出；请求日志统一包含 `request_id`、`method`、`path`、`status`、`latency_ms`。

### 5. 参数校验与响应
- 入参校验使用 `github.com/go-playground/validator/v10`，在 DTO 结构体 tag 中声明规则。
- 路由统一 `/api/v1` 前缀；健康检查为 `/healthz`；分页参数统一为 `page`、`page_size`。
- 响应统一为 `{ "code": 0, "message": "ok", "data": ... }` 包裹，错误码集中维护。

### 6. 测试与文档
- 单元测试使用标准库 `testing` + 表驱动（table-driven），文件命名 `*_test.go`；service 与 repository 必须有对应测试。
- 对外接口提供 OpenAPI/Swagger（如 `github.com/swaggo/gin-swagger`）或在 README 中列出完整 API 清单。


## 部署要求

项目必须支持 Docker Compose 一键部署，且在 **任意目录名（包括中文目录名）** 下都能正常启动。

需要提供以下文件：
- 根目录 `docker-compose.yml`
- 根目录 `.env`：包含 `COMPOSE_PROJECT_NAME=gbsched`
- `frontend/Dockerfile`：多阶段构建，Nginx 托管
- `backend/Dockerfile`：Go 多阶段构建（见下方骨架）
- `frontend/nginx.conf`
- `.env.example`

backend/Dockerfile 使用以下 Go 多阶段构建骨架：
```
FROM golang:1.22-alpine AS builder
WORKDIR /app
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 GOOS=linux go build -o /app/server ./cmd/server
FROM alpine:3.20
RUN apk add --no-cache ca-certificates tzdata
WORKDIR /app
COPY --from=builder /app/server .
EXPOSE 8080
CMD ["/app/server"]
```

docker-compose.yml 要求：
- **不要写 `version:` 字段**
- 顶层声明 `name: gbsched`
- 所有服务使用 `container_name`，带 `${COMPOSE_PROJECT_NAME:-gbsched}` 前缀
- 数据库数据使用命名卷持久化（顶层 `volumes:` 声明）
- 前端通过 Nginx 反向代理后端 API（调用 `/api`，转发到 `backend:8080`，不要硬编码 `localhost`）
- 服务间通过 Docker 内部网络通信
- 数据库配置 `healthcheck`，后端 `depends_on` 数据库 healthy
- 后端也配置 `healthcheck`，前端 `depends_on` 后端
- 暴露端口：前端 `18931:80`，后端 `19931:8080`，数据库 `3306:3306`
- 支持 `docker compose up -d` 一条命令启动

`.env` / `.env.example` 必须包含的键：
- `COMPOSE_PROJECT_NAME=gbsched`
- `DB_NAME` / `DB_USER` / `DB_PASSWORD` / `DB_ROOT_PASSWORD`
- `JWT_SECRET`
- `FRONTEND_PORT=18931`、`BACKEND_PORT=19931`

Nginx 反向代理要求：
- `location /api/` 反向代理到 `http://backend:8080/`
- `try_files $uri $uri/ /index.html;` 支持 SPA 路由

## README 写法

根目录必须有 `README.md`，用中文说明项目。包含项目名称和介绍、主要功能、快速启动方式（Docker Compose）、本地开发方式、访问地址、技术栈（表格）、项目目录结构、环境变量说明、Docker 部署说明、License。Docker 部署命令放在最前面。技术栈表格后端一栏写 `Go 1.22 + Gin + GORM`。后端本地开发命令为 `cd backend && go mod tidy && go run ./cmd/server`（或 `go run .`），构建命令为 `go build ./...`。首次启动前执行 `cp .env.example .env`。

## docker-compose.yml 参考骨架

```yaml
name: gbsched

services:
  db:
    image: mysql:8.0
    container_name: ${COMPOSE_PROJECT_NAME:-gbsched}-db
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_ROOT_PASSWORD}
      MYSQL_DATABASE: ${DB_NAME}
      MYSQL_USER: ${DB_USER}
      MYSQL_PASSWORD: ${DB_PASSWORD}
    volumes:
      - db_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-p${DB_ROOT_PASSWORD}"]
      interval: 5s
      timeout: 5s
      retries: 20

  backend:
    build: ./backend
    container_name: ${COMPOSE_PROJECT_NAME:-gbsched}-backend
    restart: unless-stopped
    environment:
      DB_HOST: db
      DB_PORT: 3306
      DB_NAME: ${DB_NAME}
      DB_USER: ${DB_USER}
      DB_PASSWORD: ${DB_PASSWORD}
      JWT_SECRET: ${JWT_SECRET}
      PORT: 8080
    ports:
      - "${BACKEND_PORT:-19931}:8080"
    depends_on:
      db:
        condition: service_healthy

  frontend:
    build: ./frontend
    container_name: ${COMPOSE_PROJECT_NAME:-gbsched}-frontend
    restart: unless-stopped
    ports:
      - "${FRONTEND_PORT:-18931}:80"
    depends_on:
      - backend

volumes:
  db_data:
```

## Git 要求

代码编写完成后初始化 Git 并创建 `.gitignore`：

```
node_modules/
dist/
.env
.DS_Store
*.log
.idea/
.vscode/
backend/server
backend/bin/
*.test
*.out
```
